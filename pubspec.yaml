name: light_on_flashlight
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.7+19

environment:
  sdk: ^3.12.0

# 코드에서 실제로 import/export 하는 패키지만 직접 의존성으로 선언한다.
# (플랫폼 구현 패키지 path_provider_android, url_launcher_web 등은
#  상위 패키지가 자동으로 끌어오므로 여기에 적지 않는다.)
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  auto_size_text: ^3.0.0
  collection: ^1.19.0
  cupertino_icons: ^1.0.0
  emoji_flag_converter: ^1.1.0
  flutter_animate: ^4.5.0
  from_css_color: ^2.0.0
  go_router: ^17.3.0
  google_fonts: ^8.2.0
  google_mobile_ads: ^9.0.0
  in_app_purchase: ^3.3.0
  # 위 규칙의 예외다. 보통 플랫폼 구현 패키지는 직접 적지 않지만, 결제 소유
  # 판정에 in_app_purchase 의 공개 API 만으로는 닿을 수 없는 값이 하나 있다.
  # restorePurchases()/queryPastPurchases() 는 응답 코드를 항상 ok 로 강제해서
  # (pigeon_converters.dart 의 forceOkResponseCode) networkError 를 절대
  # 보고하지 못한다. "안 샀다"와 "Play 에 못 물어봤다"가 구분이 안 되면
  # 정상 구매자의 권한을 뺏게 된다. 그래서 billingResult.responseCode 를
  # 직접 읽으려고 이 둘을 import 하고, import 하므로 여기에 선언한다.
  # 자세한 근거는 lib/flutter_flow/billing_util.dart 주석에 있다.
  in_app_purchase_android: ^0.5.2
  in_app_purchase_platform_interface: ^1.4.1
  intl: ^0.20.2
  json_path: ^0.9.0
  page_transition: ^2.1.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  timeago: ^3.6.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # 아이콘/스플래시 생성용 CLI 도구 (런타임에는 쓰이지 않음)
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.1


flutter_launcher_icons:
  android: 'launcher_icon'
  ios: true
  web:
    generate: true
  image_path: 'assets/images/app_launcher_icon.png'
  adaptive_icon_background: '#00000000'
  adaptive_icon_foreground: 'assets/images/adaptive_foreground_icon.png'


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/fonts/
    - assets/images/
    - assets/videos/
    - assets/audios/
    - assets/rive_animations/
    - assets/pdfs/
    - assets/jsons/



  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

