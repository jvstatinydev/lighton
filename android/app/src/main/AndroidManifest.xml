<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <uses-permission android:name="android.permission.INTERNET"/>
  <!-- 홈 화면 위젯으로 켠 손전등을 붙들어 두는 포그라운드 서비스(TorchService.kt).
       타입이 specialUse 라 Play Console 의 "포그라운드 서비스 권한" 신고가 필요하다. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
  <application
    android:name=".LightOnApplication"
    android:label="@string/app_name"
    tools:replace="android:label"
    android:icon="@mipmap/ic_launcher"
    android:requestLegacyExternalStorage="true"
  >
    <activity android:name=".MainActivity" android:exported="true" android:launchMode="singleTop" android:theme="@style/LaunchTheme" android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode" android:hardwareAccelerated="true" android:windowSoftInputMode="adjustResize">
      <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
      <meta-data android:name="io.flutter.embedding.android.NormalTheme" android:resource="@style/NormalTheme"/>
      <!-- Displays an Android View that continues showing the launch screen
                 Drawable until Flutter paints its first frame, then this splash
                 screen fades out. A splash screen is useful to avoid any visual
                 gap between the end of Android's launch screen and the painting of
                 Flutter's first frame. -->
      <meta-data android:name="io.flutter.embedding.android.SplashScreenDrawable" android:resource="@drawable/launch_background"/>
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <!-- Deep linking -->
      <meta-data android:name="flutter_deeplinking_enabled" android:value="true"/>
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="lightonflashlight" android:host="lightonflashlight.com"/>
      </intent-filter>
    </activity>
    <!-- 홈 화면 위젯. 크기마다 하나씩 등록해 위젯 목록에 "작게/중간/넓게/크게" 가
         따로 보이고, 고른 크기 그대로 놓인다(TorchWidgetProvider.kt).
         exported="true" 는 위젯 리시버의 관례다. 다른 앱이 토글 액션을 보낼 수는
         있지만, 토치는 원래 권한 없이 아무 앱이나 켤 수 있어 새로 열리는 것은 없다. -->
    <receiver android:name=".TorchWidget1x1" android:exported="true" android:label="@string/widget_label_1x1">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
      </intent-filter>
      <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_torch_1x1"/>
    </receiver>
    <receiver android:name=".TorchWidget2x2" android:exported="true" android:label="@string/widget_label_2x2">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
      </intent-filter>
      <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_torch_2x2"/>
    </receiver>
    <receiver android:name=".TorchWidget4x2" android:exported="true" android:label="@string/widget_label_4x2">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
      </intent-filter>
      <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_torch_4x2"/>
    </receiver>
    <receiver android:name=".TorchWidget4x4" android:exported="true" android:label="@string/widget_label_4x4">
      <intent-filter>
        <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
      </intent-filter>
      <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_torch_4x4"/>
    </receiver>
    <!-- 위젯으로 켠 손전등을 붙들어 두는 포그라운드 서비스. 프로세스가 죽으면
         카메라 서비스가 토치를 끄기 때문에 필요하다. TorchService.kt 참고. -->
    <service android:name=".TorchService" android:exported="false" android:foregroundServiceType="specialUse">
      <property android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE" android:value="flashlight"/>
    </service>
    <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
    <meta-data android:name="flutterEmbedding" android:value="2"/>
    <meta-data android:name="com.google.android.gms.ads.APPLICATION_ID" android:value="ca-app-pub-3228085068090706~2375958262"/>
    <!-- 언어별 앱 이름은 res/values-XX/strings.xml 의 app_name 이 담당한다.
             (application 의 android:label="@string/app_name" 이 그것을 참조한다)
             예전에는 같은 목적으로 activity-alias 를 나열했지만, MAIN/LAUNCHER
             intent-filter 가 없어 런처에 노출되지 않는 죽은 선언이었으므로 제거했다. -->
  </application>
</manifest>